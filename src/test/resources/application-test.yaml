spring:
  datasource:
    url: jdbc:h2:mem:test;DB_CLOSE_ON_EXIT=FALSE
    username: test
    password: test
  jpa:
    hibernate:
      ddl-auto: create-drop
    defer-datasource-initialization: true

test:
  url: "http://localhost:"